# AF05149663
The Hotel Management System is a Java-based application using JDBC and MySQL to manage hotel records. It allows users to perform CRUD operations such as adding, viewing, updating, and deleting hotel details. The system uses a modular structure with DAO and ensures efficient database interaction.
